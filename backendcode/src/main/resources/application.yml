server:
  port: 8080

spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: running-app-service
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb+srv://dohuynhan2408_db_user:qnBGPJ9R9Qo9Zm46@running-app-cluster.4hcp7lk.mongodb.net/running_app_db?appName=running-app-cluster}
      database: running_app_db

app:
  jwt:
    secret: ${JWT_SECRET:?Set JWT_SECRET in your environment (.env) to a 32+ character random string}
    expiration-millis: ${JWT_EXPIRATION_MILLIS:3600000}
